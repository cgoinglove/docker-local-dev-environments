{
  "name": "docker-compose",
  "version": "1.0.0",
  "description": "compose setup",
  "author": "cgoing",
  "license": "MIT",
  "scripts": {
    "kafka": "docker-compose -p local-kafka -f docker-compose.kafka.yml up",
    "redis-cluster": "docker-compose -p local-redis-cluster -f docker-compose.redis-cluster.yml up"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "opn": "^6.0.0"
  }
}
